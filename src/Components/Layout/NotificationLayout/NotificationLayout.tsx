import "react-toastify/dist/ReactToastify.css";

import React from "react";
import { ToastContainer } from "react-toastify";

const NotificationLayout = (): JSX.Element => {
    return <ToastContainer />;
};

export default NotificationLayout;
